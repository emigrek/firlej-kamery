import styles from "./styles";

const options = {
    backgroundColor: "#171717",
    disableDefaultUI: true,
    styles: styles,
    zoomControl: false,
    minZoom: 14,
    maxZoom: 18
}

export default options;